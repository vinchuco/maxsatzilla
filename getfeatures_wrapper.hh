#ifndef GETFEATURES_WRAPPER_HH
#define GETFEATURES_WRAPPER_HH

#include <string>
#include <map>

using std::string;
using std::map;

map<string, double> getFeatures(const string &inst);

#endif // GETFEATURES_WRAPPER_HH
